[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    unit: fast isolated tests for local components
    contract: tests that enforce CLI and schema contracts
    integration: cross-module tests for orchestration flows
    e2e: end-to-end tests across full session lifecycle
