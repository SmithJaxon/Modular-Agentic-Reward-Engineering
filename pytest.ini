[pytest]
testpaths = tests
markers =
    unit: fast isolated validation of individual modules.
    contract: contract and schema validation for public interfaces.
    integration: multi-component workflow validation.
    e2e: end-to-end scenario validation.
